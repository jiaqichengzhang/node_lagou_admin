{{each data}}
<tr>
    <td>{{$index+1}} </td>
    <td>{{$value.username}}</td>
    <td>
        <button class="btn btn-danger btn-del" data-userid="{{$value._id}}">删除</button>
    </td>
</tr>
{{/each}}
